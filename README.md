# 5Aether.
Aether Phase 1 Core: A modular Python 3.12 desktop architecture running a localized computer vision pipeline. It captures live camera frames, executes YOLOv8 object detection, computes 3D pose/distance metrics via OpenCV solvePnP, and outputs a contextual HUD overlay. All speech, cloud, and LLM features are explicitly excluded.
